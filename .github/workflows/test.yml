name: Automatically create PR in Tinderweb on new release

on:
  push:
    branches:
      - "*"

jobs:
  release:
    name: "Dispatch new tag release to TinderWeb"
    runs-on: ubuntu-latest
    concurrency: release

    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v2.1.1
        with:
          token: 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
          repository: tinder-indumunnangi/TestRepo1
          event-type: my-event
          client-payload: '{"ref": "${{ github.ref }}", "tag": "tagTest"}'
